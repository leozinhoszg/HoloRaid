# Internacionalização do HoloRaid — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o inglês a língua-base de toda a stack e dar ao app Flutter suporte a 5 idiomas (en padrão + pt/de/fr/es), com auto-detecção do dispositivo, seletor manual e persistência.

**Architecture:** `easy_localization` envolve o app; traduções em JSON por idioma em `assets/translations/`. `en.json` é a fonte canônica; os demais têm paridade total de chaves (garantida por teste). Cada tela migra literais de UI para `'chave'.tr()`. Backend tem suas mensagens PT substituídas por inglês, sem framework.

**Tech Stack:** Flutter, Riverpod, go_router, `easy_localization`; backend TypeScript/Node.

## Global Constraints

- Idioma padrão/fallback do app: **`en`**. Suportados: `en`, `pt`, `de`, `fr`, `es`.
- Nomes de idioma no seletor: nativos, **sem** bandeiras/emojis — `English`, `Português`, `Deutsch`, `Français`, `Español`.
- `en.json` é a **fonte canônica**; todos os outros JSONs devem ter **exatamente as mesmas chaves** (nem faltando, nem sobrando).
- **Não** internacionalizar: caminhos de asset, chaves de API, e keys internas de dados/serialização (`'today'`, `'week'`, `'month'`, `'operation'`, `'count'`, `'username'`, `'confirmed'`, `'waitlist'` etc.).
- Backend: só substituir **texto legível** ao usuário; manter códigos/chaves de erro internos.
- Dart SDK do projeto: `^3.11.5`. Rodar comandos Flutter pelo **PowerShell**, não git-bash (gotcha conhecido). Porta backend dev: 3010.
- Commits frequentes, um por tarefa concluída.

---

## File Structure

**App — criar:**
- `app/assets/translations/en.json` — fonte canônica (inglês)
- `app/assets/translations/pt.json`, `de.json`, `fr.json`, `es.json` — traduções
- `app/lib/core/settings/language_selector.dart` — widget `LanguageSelector`
- `app/test/l10n_key_parity_test.dart` — teste de paridade de chaves
- `app/test/support/localized_tester.dart` — helper de teste que envolve widgets com `EasyLocalization`

**App — modificar:**
- `app/pubspec.yaml` — dependência + assets
- `app/lib/main.dart` — bootstrap + wiring do `MaterialApp.router`
- Telas/widgets (migração de strings): `features/login/login_screen.dart`, `core/nav/nav_destinations.dart`, `core/nav/holo_user_menu.dart`, `features/splash/splash_screen.dart`, `features/dashboard/dashboard_screen.dart`, `features/home/home_screen.dart`, `features/home/widgets/next_raid_hero.dart`, `features/raids/raids_list_screen.dart`, `features/raids/raid_detail_screen.dart`, `features/raids/raid_form_screen.dart`, `features/characters/characters_list_screen.dart`, `features/characters/character_form_screen.dart`, `features/characters/character_profile_screen.dart`, `features/profile/profile_screen.dart`, `features/profile/me_progression_screen.dart`, `features/admin/users_admin_screen.dart`

**Backend — modificar:**
- `backend/src/common/errors/AppError.ts`, `common/middleware/errorHandler.ts`, `common/middleware/validate.ts`, `common/security/jwt.ts`, `discord/discordSync.ts`, e `modules/**/*.service.ts` + `modules/**/*.schemas.ts`

---

## Task 1: Instalar easy_localization + scaffolding de traduções + wiring

**Files:**
- Modify: `app/pubspec.yaml`
- Create: `app/assets/translations/{en,pt,de,fr,es}.json`
- Modify: `app/lib/main.dart`

**Interfaces:**
- Produces: app rodando sob `EasyLocalization`; `'chave'.tr()` disponível globalmente via `import 'package:easy_localization/easy_localization.dart';`.

- [ ] **Step 1: Adicionar dependência**

Em `app/pubspec.yaml`, sob `dependencies:` (após `url_launcher`):

```yaml
  easy_localization: ^3.0.7
```

Em `app/pubspec.yaml`, sob `flutter:` → `assets:` (criar a seção se não existir):

```yaml
  assets:
    - assets/translations/
```

Rodar (PowerShell): `cd app; flutter pub get`
Expected: resolve sem erro.

- [ ] **Step 2: Criar os 5 JSONs com uma chave-semente**

Cria cada arquivo com uma chave comum inicial para validar o pipeline.

`app/assets/translations/en.json`:
```json
{ "common": { "app_name": "HoloRaid" } }
```
`app/assets/translations/pt.json`:
```json
{ "common": { "app_name": "HoloRaid" } }
```
`app/assets/translations/de.json`:
```json
{ "common": { "app_name": "HoloRaid" } }
```
`app/assets/translations/fr.json`:
```json
{ "common": { "app_name": "HoloRaid" } }
```
`app/assets/translations/es.json`:
```json
{ "common": { "app_name": "HoloRaid" } }
```

- [ ] **Step 3: Wiring em `main.dart`**

Substituir o conteúdo de `app/lib/main.dart` por:

```dart
import 'package:easy_localization/easy_localization.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'core/router/app_router.dart';
import 'core/ui/holo_background.dart';
import 'core/ui/holo_theme.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await EasyLocalization.ensureInitialized();
  runApp(
    EasyLocalization(
      supportedLocales: const [
        Locale('en'), Locale('pt'), Locale('de'), Locale('fr'), Locale('es'),
      ],
      path: 'assets/translations',
      fallbackLocale: const Locale('en'),
      useOnlyLangCode: true,
      child: const ProviderScope(child: HoloRaidApp()),
    ),
  );
}

class HoloRaidApp extends ConsumerWidget {
  const HoloRaidApp({super.key});
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(routerProvider);
    return MaterialApp.router(
      title: 'HoloRaid',
      theme: holoTheme(),
      routerConfig: router,
      locale: context.locale,
      supportedLocales: context.supportedLocales,
      localizationsDelegates: context.localizationDelegates,
      builder: (context, child) => HoloBackground(child: child ?? const SizedBox.shrink()),
    );
  }
}
```

- [ ] **Step 4: Rodar o app para validar boot**

Run (PowerShell): `cd app; flutter run -d chrome` (ou build web). Alternativa headless: `cd app; flutter analyze`
Expected: `flutter analyze` sem erros novos; app builda e abre em inglês.

- [ ] **Step 5: Commit**

```bash
git add app/pubspec.yaml app/pubspec.lock app/assets/translations app/lib/main.dart
git commit -m "feat(app): integra easy_localization (base en, 5 idiomas)"
```

---

## Task 2: Teste de paridade de chaves + helper de teste localizado

**Files:**
- Create: `app/test/l10n_key_parity_test.dart`
- Create: `app/test/support/localized_tester.dart`

**Interfaces:**
- Produces: `pumpLocalized(WidgetTester, Widget, {Locale locale})` — helper para widget tests envolverem a árvore com `EasyLocalization`.
- Consumes: os 5 JSONs de `assets/translations`.

- [ ] **Step 1: Escrever o teste de paridade (falhando de propósito é opcional — ele já deve passar com os stubs)**

`app/test/l10n_key_parity_test.dart`:
```dart
import 'dart:convert';
import 'dart:io';
import 'package:flutter_test/flutter_test.dart';

/// Achata um mapa aninhado em chaves com ponto: {"a":{"b":1}} -> {"a.b"}.
Set<String> _flatKeys(Map<String, dynamic> m, [String prefix = '']) {
  final out = <String>{};
  m.forEach((k, v) {
    final key = prefix.isEmpty ? k : '$prefix.$k';
    if (v is Map<String, dynamic>) {
      out.addAll(_flatKeys(v, key));
    } else {
      out.add(key);
    }
  });
  return out;
}

Set<String> _keysOf(String lang) {
  final raw = File('assets/translations/$lang.json').readAsStringSync();
  return _flatKeys(json.decode(raw) as Map<String, dynamic>);
}

void main() {
  test('todos os idiomas têm exatamente as mesmas chaves que en', () {
    final en = _keysOf('en');
    for (final lang in ['pt', 'de', 'fr', 'es']) {
      final other = _keysOf(lang);
      final missing = en.difference(other);
      final extra = other.difference(en);
      expect(missing, isEmpty, reason: '$lang está SEM as chaves: $missing');
      expect(extra, isEmpty, reason: '$lang tem chaves A MAIS: $extra');
    }
  });
}
```

- [ ] **Step 2: Rodar o teste**

Run (PowerShell): `cd app; flutter test test/l10n_key_parity_test.dart`
Expected: PASS (stubs têm a mesma única chave).

- [ ] **Step 3: Criar helper de teste localizado**

`app/test/support/localized_tester.dart`:
```dart
import 'package:easy_localization/easy_localization.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

/// Envolve [child] com EasyLocalization para widget tests.
/// Chame `await EasyLocalization.ensureInitialized()` no setUp do teste.
Future<void> pumpLocalized(
  WidgetTester tester,
  Widget child, {
  Locale locale = const Locale('en'),
}) async {
  await tester.pumpWidget(
    EasyLocalization(
      supportedLocales: const [
        Locale('en'), Locale('pt'), Locale('de'), Locale('fr'), Locale('es'),
      ],
      path: 'assets/translations',
      fallbackLocale: const Locale('en'),
      useOnlyLangCode: true,
      startLocale: locale,
      child: MaterialApp(
        locale: locale,
        supportedLocales: const [
          Locale('en'), Locale('pt'), Locale('de'), Locale('fr'), Locale('es'),
        ],
        localizationsDelegates: const [],
        home: child,
      ),
    ),
  );
  await tester.pumpAndSettle();
}
```
> Nota: em `setUpAll` dos widget tests, chamar `TestWidgetsFlutterBinding.ensureInitialized()` e `await EasyLocalization.ensureInitialized()`. Ajustar delegates conforme o `EasyLocalization` injeta em runtime (o `context.localizationDelegates` real vem do widget pai).

- [ ] **Step 4: Commit**

```bash
git add app/test/l10n_key_parity_test.dart app/test/support/localized_tester.dart
git commit -m "test(app): paridade de chaves i18n + helper de teste localizado"
```

---

## Task 3: LanguageSelector + plugar em Login e Perfil

**Files:**
- Create: `app/lib/core/settings/language_selector.dart`
- Modify: `app/lib/features/login/login_screen.dart` (adicionar seletor discreto no footer)
- Modify: `app/lib/features/profile/profile_screen.dart` (adicionar item de idioma)
- Modify: `app/assets/translations/*.json` (chaves `common.language`)

**Interfaces:**
- Produces: `class LanguageSelector extends StatelessWidget` — dropdown que chama `context.setLocale(...)`.
- Consumes: `easy_localization` (`context.locale`, `context.setLocale`).

- [ ] **Step 1: Adicionar chaves de idioma aos 5 JSONs**

Em cada `*.json`, dentro de `common`, adicionar a chave `language` (rótulo do campo), traduzida. Os **nomes dos idiomas em si permanecem nativos** e ficam no código (não traduzidos).

`en.json` → `common`: `"language": "Language"`
`pt.json` → `common`: `"language": "Idioma"`
`de.json` → `common`: `"language": "Sprache"`
`fr.json` → `common`: `"language": "Langue"`
`es.json` → `common`: `"language": "Idioma"`

- [ ] **Step 2: Criar o widget**

`app/lib/core/settings/language_selector.dart`:
```dart
import 'package:easy_localization/easy_localization.dart';
import 'package:flutter/material.dart';

/// Nomes nativos (não traduzidos) de cada idioma suportado.
const Map<String, String> kLanguageNames = {
  'en': 'English',
  'pt': 'Português',
  'de': 'Deutsch',
  'fr': 'Français',
  'es': 'Español',
};

/// Dropdown de seleção de idioma. Persiste automaticamente (easy_localization).
class LanguageSelector extends StatelessWidget {
  const LanguageSelector({super.key, this.compact = false});

  /// Quando true, renderiza sem label (para uso discreto no login).
  final bool compact;

  @override
  Widget build(BuildContext context) {
    final current = context.locale.languageCode;
    final dropdown = DropdownButton<String>(
      value: kLanguageNames.containsKey(current) ? current : 'en',
      underline: const SizedBox.shrink(),
      icon: const Icon(Icons.language, size: 18),
      items: kLanguageNames.entries
          .map((e) => DropdownMenuItem(value: e.key, child: Text(e.value)))
          .toList(),
      onChanged: (code) {
        if (code != null) context.setLocale(Locale(code));
      },
    );
    if (compact) return dropdown;
    return Row(
      mainAxisSize: MainAxisSize.min,
      children: [
        Text('${'common.language'.tr()}: '),
        dropdown,
      ],
    );
  }
}
```

- [ ] **Step 3: Plugar no Login**

Em `app/lib/features/login/login_screen.dart`, dentro de `_footer`, após o `Text('Not affiliated…')`, adicionar o seletor discreto. Importar `import '../../core/settings/language_selector.dart';`. No `Column` do `_footer`, acrescentar:
```dart
      const SizedBox(height: 12),
      const LanguageSelector(compact: true),
```

- [ ] **Step 4: Plugar no Perfil**

Em `app/lib/features/profile/profile_screen.dart`, importar `import '../../core/settings/language_selector.dart';`. Ao final do `ListView` (após o `FutureBuilder` de raids), adicionar:
```dart
              const SizedBox(height: 20),
              const Divider(),
              const Padding(
                padding: EdgeInsets.symmetric(vertical: 8),
                child: LanguageSelector(),
              ),
```

- [ ] **Step 5: Widget test do seletor**

Criar `app/test/language_selector_test.dart`:
```dart
import 'package:easy_localization/easy_localization.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:holoraid/core/settings/language_selector.dart';
import 'support/localized_tester.dart';

void main() {
  setUpAll(() async {
    TestWidgetsFlutterBinding.ensureInitialized();
    await EasyLocalization.ensureInitialized();
  });

  testWidgets('mostra os 5 idiomas nativos', (tester) async {
    await pumpLocalized(tester, const Scaffold(body: LanguageSelector()));
    await tester.tap(find.byType(LanguageSelector));
    await tester.pumpAndSettle();
    for (final name in ['English', 'Português', 'Deutsch', 'Français', 'Español']) {
      expect(find.text(name), findsWidgets);
    }
  });
}
```

- [ ] **Step 6: Rodar testes + analyze**

Run (PowerShell): `cd app; flutter test test/language_selector_test.dart; flutter analyze`
Expected: teste PASS; analyze sem erros novos.

- [ ] **Step 7: Commit**

```bash
git add app/lib/core/settings/language_selector.dart app/lib/features/login/login_screen.dart app/lib/features/profile/profile_screen.dart app/assets/translations app/test/language_selector_test.dart
git commit -m "feat(app): LanguageSelector no login e no perfil"
```

---

## Task 4: Migrar tela de Login (padrão de referência para as demais)

Esta tarefa demonstra o **procedimento mecânico** que todas as tarefas de migração (5–8) seguem: (a) para cada literal de UI da tela, criar uma chave no namespace da tela em `en.json`; (b) preencher pt/de/fr/es com a tradução; (c) trocar o literal por `'chave'.tr()`; (d) manter paridade e rodar o teste.

**Files:**
- Modify: `app/lib/features/login/login_screen.dart`
- Modify: `app/assets/translations/*.json` (namespace `login`)

**Interfaces:**
- Consumes: `easy_localization` (`import 'package:easy_localization/easy_localization.dart';`).

- [ ] **Step 1: Adicionar o namespace `login` aos 5 JSONs**

`en.json` (merge dentro do objeto raiz):
```json
"login": {
  "tagline": "Command your SWTOR Operations.",
  "subtitle": "Organize raids, sync with Discord, and track your PvE progression — in real time.",
  "connecting": "Connecting…",
  "continue_discord": "Continue with Discord",
  "login_failed": "Login failed. Try again.",
  "highlight_organize": "Organize raids",
  "highlight_sync": "Sync with Discord",
  "highlight_track": "Track PvE progression",
  "terms": "Terms",
  "privacy": "Privacy",
  "not_affiliated": "Not affiliated with BioWare or EA."
}
```
`pt.json`:
```json
"login": {
  "tagline": "Comande suas Operations de SWTOR.",
  "subtitle": "Organize raids, sincronize com o Discord e acompanhe sua progressão PvE — em tempo real.",
  "connecting": "Conectando…",
  "continue_discord": "Continuar com Discord",
  "login_failed": "Falha no login. Tente novamente.",
  "highlight_organize": "Organize raids",
  "highlight_sync": "Sincronize com o Discord",
  "highlight_track": "Acompanhe a progressão PvE",
  "terms": "Termos",
  "privacy": "Privacidade",
  "not_affiliated": "Sem afiliação com a BioWare ou a EA."
}
```
`de.json`:
```json
"login": {
  "tagline": "Führe deine SWTOR-Operationen an.",
  "subtitle": "Organisiere Raids, synchronisiere mit Discord und verfolge deinen PvE-Fortschritt — in Echtzeit.",
  "connecting": "Verbinde…",
  "continue_discord": "Mit Discord fortfahren",
  "login_failed": "Anmeldung fehlgeschlagen. Bitte erneut versuchen.",
  "highlight_organize": "Raids organisieren",
  "highlight_sync": "Mit Discord synchronisieren",
  "highlight_track": "PvE-Fortschritt verfolgen",
  "terms": "Nutzungsbedingungen",
  "privacy": "Datenschutz",
  "not_affiliated": "Nicht mit BioWare oder EA verbunden."
}
```
`fr.json`:
```json
"login": {
  "tagline": "Commandez vos Opérations SWTOR.",
  "subtitle": "Organisez des raids, synchronisez avec Discord et suivez votre progression PvE — en temps réel.",
  "connecting": "Connexion…",
  "continue_discord": "Continuer avec Discord",
  "login_failed": "Échec de la connexion. Réessayez.",
  "highlight_organize": "Organiser des raids",
  "highlight_sync": "Synchroniser avec Discord",
  "highlight_track": "Suivre la progression PvE",
  "terms": "Conditions",
  "privacy": "Confidentialité",
  "not_affiliated": "Sans affiliation avec BioWare ou EA."
}
```
`es.json`:
```json
"login": {
  "tagline": "Comanda tus Operaciones de SWTOR.",
  "subtitle": "Organiza raids, sincroniza con Discord y sigue tu progresión PvE — en tiempo real.",
  "connecting": "Conectando…",
  "continue_discord": "Continuar con Discord",
  "login_failed": "Error al iniciar sesión. Inténtalo de nuevo.",
  "highlight_organize": "Organiza raids",
  "highlight_sync": "Sincroniza con Discord",
  "highlight_track": "Sigue la progresión PvE",
  "terms": "Términos",
  "privacy": "Privacidad",
  "not_affiliated": "Sin afiliación con BioWare ni EA."
}
```

- [ ] **Step 2: Trocar os literais por `.tr()` em `login_screen.dart`**

Adicionar no topo: `import 'package:easy_localization/easy_localization.dart';`

Substituições (remover `const` onde o texto passa a ser `.tr()`):
- Linha 30: `_error = 'Login failed. Try again.'` → `_error = 'login.login_failed'.tr()`
- Linhas 102-106: `Text('Command your SWTOR Operations.', …)` → `Text('login.tagline'.tr(), …)` (remover `const` do `Text`)
- Linhas 108-112: `Text('Organize raids, sync with Discord…', …)` → `Text('login.subtitle'.tr(), …)`
- Linha 152: `Text(_loading ? 'Connecting…' : 'Continue with Discord')` → `Text(_loading ? 'login.connecting'.tr() : 'login.continue_discord'.tr())`
- Linhas 161-163 (highlights): trocar os 3 literais por `'login.highlight_organize'.tr()`, `'login.highlight_sync'.tr()`, `'login.highlight_track'.tr()` (a lista deixa de ser `const`)
- Linha 184: `const Text('Terms', style: link)` → `Text('login.terms'.tr(), style: link)`
- Linha 186: `const Text('Privacy', style: link)` → `Text('login.privacy'.tr(), style: link)`
- Linhas 189-190: `const Text('Not affiliated with BioWare or EA.', …)` → `Text('login.not_affiliated'.tr(), …)`

- [ ] **Step 3: Rodar paridade + analyze**

Run (PowerShell): `cd app; flutter test test/l10n_key_parity_test.dart; flutter analyze`
Expected: paridade PASS; analyze sem erros novos.

- [ ] **Step 4: Commit**

```bash
git add app/lib/features/login/login_screen.dart app/assets/translations
git commit -m "i18n(app): migra tela de login (namespace login)"
```

---

## Task 5: Migrar navegação + splash + common

**Files:**
- Modify: `app/lib/core/nav/nav_destinations.dart`, `app/lib/core/nav/holo_user_menu.dart`, `app/lib/features/splash/splash_screen.dart`
- Modify: `app/assets/translations/*.json` (namespaces `nav`, `splash`, `common`)

**Procedimento (idêntico à Task 4):** para cada literal de UI destes arquivos, criar chave no namespace apropriado (`nav.*` para rótulos de navegação/menu, `splash.*` para a splash, `common.*` para termos reutilizáveis como `save`/`cancel`/`delete`/`close`/`no_data`), traduzir nos 5 idiomas com paridade, e trocar por `'chave'.tr()` com `import 'package:easy_localization/easy_localization.dart';`.

- [ ] **Step 1: Ler os arquivos e listar todos os literais de UI**

Run: abrir `nav_destinations.dart`, `holo_user_menu.dart`, `splash_screen.dart` e anotar cada string voltada ao usuário (rótulos de aba, itens de menu como "Logout", textos da splash). **Não** incluir keys internas/rotas.

- [ ] **Step 2: Adicionar as chaves nos 5 JSONs (paridade total)**

Criar `nav.*`, `splash.*` e as `common.*` necessárias em `en.json` e traduzir em pt/de/fr/es. Ex. de `common` reutilizável:
`en`: `"logout": "Logout"`, `"loading": "Loading…"`, `"save": "Save"`, `"cancel": "Cancel"`, `"delete": "Delete"`, `"close": "Close"`, `"no_data": "No data yet."`
`pt`: `"Sair"`, `"Carregando…"`, `"Salvar"`, `"Cancelar"`, `"Excluir"`, `"Fechar"`, `"Sem dados ainda."`
`de`: `"Abmelden"`, `"Lädt…"`, `"Speichern"`, `"Abbrechen"`, `"Löschen"`, `"Schließen"`, `"Noch keine Daten."`
`fr`: `"Déconnexion"`, `"Chargement…"`, `"Enregistrer"`, `"Annuler"`, `"Supprimer"`, `"Fermer"`, `"Aucune donnée."`
`es`: `"Cerrar sesión"`, `"Cargando…"`, `"Guardar"`, `"Cancelar"`, `"Eliminar"`, `"Cerrar"`, `"Sin datos aún."`

- [ ] **Step 3: Trocar literais por `.tr()` nos 3 arquivos**

Aplicar o padrão da Task 4 Step 2 em cada arquivo.

- [ ] **Step 4: Paridade + analyze**

Run (PowerShell): `cd app; flutter test test/l10n_key_parity_test.dart; flutter analyze`
Expected: PASS; sem erros novos.

- [ ] **Step 5: Commit**

```bash
git add app/lib/core/nav app/lib/features/splash app/assets/translations
git commit -m "i18n(app): migra navegacao, menu e splash"
```

---

## Task 6: Migrar Dashboard + Home

**Files:**
- Modify: `app/lib/features/dashboard/dashboard_screen.dart`, `app/lib/features/home/home_screen.dart`, `app/lib/features/home/widgets/next_raid_hero.dart`
- Modify: `app/assets/translations/*.json` (namespaces `dashboard`, `home`)

**Strings conhecidas do dashboard a normalizar (hoje em PT):** `'Hoje'`→today, `'Semana'`→this week, `'Operations mais jogadas'`→"Most played Operations", `'Sem dados ainda.'`→usar `common.no_data`, `'Jogadores mais ativos'`→"Most active players", `'participantsThisMonth'` label. **Não tocar** nas keys internas `'today'`/`'week'`/`'month'`/`'operation'`/`'count'`/`'username'` (são identificadores de dados).

- [ ] **Step 1: Ler os 3 arquivos e listar literais de UI (excluindo keys internas)**

- [ ] **Step 2: Adicionar `dashboard.*` e `home.*` nos 5 JSONs (paridade)**

`en` (dashboard, exemplo): `"most_played_ops": "Most played Operations"`, `"most_active_players": "Most active players"`, `"today": "Today"`, `"this_week": "This week"`, `"this_month": "This month"`, `"participants_this_month": "participants this month"`.
`pt`: `"Operations mais jogadas"`, `"Jogadores mais ativos"`, `"Hoje"`, `"Esta semana"`, `"Este mês"`, `"participantes neste mês"`.
`de`: `"Meistgespielte Operationen"`, `"Aktivste Spieler"`, `"Heute"`, `"Diese Woche"`, `"Dieser Monat"`, `"Teilnehmer diesen Monat"`.
`fr`: `"Opérations les plus jouées"`, `"Joueurs les plus actifs"`, `"Aujourd'hui"`, `"Cette semaine"`, `"Ce mois-ci"`, `"participants ce mois-ci"`.
`es`: `"Operaciones más jugadas"`, `"Jugadores más activos"`, `"Hoy"`, `"Esta semana"`, `"Este mes"`, `"participantes este mes"`.
> Atenção: as chaves de UI `dashboard.today`/`this_week` são DIFERENTES das keys de dados `'today'`/`'week'` usadas na lógica de agregação — estas últimas não mudam.

- [ ] **Step 3: Trocar literais de UI por `.tr()` (padrão da Task 4)**

- [ ] **Step 4: Paridade + analyze**

Run (PowerShell): `cd app; flutter test test/l10n_key_parity_test.dart; flutter analyze`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add app/lib/features/dashboard app/lib/features/home app/assets/translations
git commit -m "i18n(app): migra dashboard e home"
```

---

## Task 7: Migrar Raids (list, detail, form)

**Files:**
- Modify: `app/lib/features/raids/raids_list_screen.dart`, `raid_detail_screen.dart`, `raid_form_screen.dart`
- Modify: `app/assets/translations/*.json` (namespaces `raids`, `raid_detail`, `raid_form`)

**Strings conhecidas (PT hoje):** `'Nenhuma raid ainda.'`→`common.no_data` ou `raids.empty`, e ~31 literais em `raid_detail` + ~18 em `raid_form`. Status/roles vindos da API (`'confirmed'`, `'waitlist'`, `'open'`) que sejam **identificadores** não mudam; se forem exibidos ao usuário, mapear para labels via `common.*` (ex.: `common.confirmed`, `common.waitlist`).

- [ ] **Step 1: Ler os 3 arquivos e listar todos os literais de UI**

Anotar cada string voltada ao usuário. Marcar quais são status/labels reutilizáveis (vão para `common.*`) vs específicas da tela.

- [ ] **Step 2: Adicionar `raids.*`, `raid_detail.*`, `raid_form.*` + `common` de status nos 5 JSONs (paridade)**

Exemplo de labels de status reutilizáveis:
`en`: `"confirmed": "Confirmed"`, `"waitlist": "Waitlist"`, `"creator": "Creator"`, `"open": "Open"`, `"closed": "Closed"`.
`pt`: `"Confirmado"`, `"Waitlist"`, `"Criador"`, `"Aberta"`, `"Fechada"`.
`de`: `"Bestätigt"`, `"Warteliste"`, `"Ersteller"`, `"Offen"`, `"Geschlossen"`.
`fr`: `"Confirmé"`, `"Liste d'attente"`, `"Créateur"`, `"Ouverte"`, `"Fermée"`.
`es`: `"Confirmado"`, `"Lista de espera"`, `"Creador"`, `"Abierta"`, `"Cerrada"`.
Demais strings específicas de detail/form: criar em `raid_detail.*`/`raid_form.*`, traduzir nos 5.

- [ ] **Step 3: Trocar literais por `.tr()` (padrão da Task 4). Para textos com valores dinâmicos, usar `'raid_form.some_key'.tr(namedArgs: {'n': '$count'})` e no JSON usar `{n}`.**

- [ ] **Step 4: Paridade + analyze**

Run (PowerShell): `cd app; flutter test test/l10n_key_parity_test.dart; flutter analyze`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add app/lib/features/raids app/assets/translations
git commit -m "i18n(app): migra telas de raids (list, detail, form)"
```

---

## Task 8: Migrar Characters + Profile + Progression + Admin

**Files:**
- Modify: `app/lib/features/characters/characters_list_screen.dart`, `character_form_screen.dart`, `character_profile_screen.dart`, `app/lib/features/profile/profile_screen.dart`, `me_progression_screen.dart`, `app/lib/features/admin/users_admin_screen.dart`
- Modify: `app/assets/translations/*.json` (namespaces `characters`, `character_form`, `character_profile`, `profile`, `progression`, `admin`)

**Strings conhecidas do Profile (PT hoje):** `'Perfil'`→"Profile", `'Papel: {role}'`→"Role: {role}", `'{n} pontos'`→"{n} points", `'faltam {n} para o próximo Tier'`→"{n} to next Tier", `'Tier máximo!'`→"Max Tier!", `'Minhas raids'`→"My raids", `'Você ainda não participou de raids.'`→"You haven't joined any raids yet." (badges `'Criador'`/`'Confirmado'`/`'Waitlist'` reusam `common.*` da Task 7).

- [ ] **Step 1: Ler os 6 arquivos e listar literais de UI**

- [ ] **Step 2: Adicionar os namespaces nos 5 JSONs (paridade). Exemplo Profile:**

`en profile`: `"title": "Profile"`, `"role": "Role: {role}"`, `"points": "{n} points"`, `"to_next_tier": "{n} to next Tier"`, `"max_tier": "Max Tier!"`, `"my_raids": "My raids"`, `"no_raids": "You haven't joined any raids yet."`
`pt`: `"Perfil"`, `"Papel: {role}"`, `"{n} pontos"`, `"faltam {n} para o próximo Tier"`, `"Tier máximo!"`, `"Minhas raids"`, `"Você ainda não participou de raids."`
`de`: `"Profil"`, `"Rolle: {role}"`, `"{n} Punkte"`, `"{n} bis zum nächsten Tier"`, `"Höchster Tier!"`, `"Meine Raids"`, `"Du bist noch keinem Raid beigetreten."`
`fr`: `"Profil"`, `"Rôle : {role}"`, `"{n} points"`, `"{n} avant le prochain palier"`, `"Palier maximum !"`, `"Mes raids"`, `"Vous n'avez encore rejoint aucun raid."`
`es`: `"Perfil"`, `"Rol: {role}"`, `"{n} puntos"`, `"{n} para el próximo Tier"`, `"¡Tier máximo!"`, `"Mis raids"`, `"Aún no te has unido a ningún raid."`

- [ ] **Step 3: Trocar literais por `.tr()`. Para os textos com `{role}`/`{n}`, usar `namedArgs`:**

Exemplos em `profile_screen.dart`:
- Linha 33: `AppBar(title: const Text('Perfil'))` → `AppBar(title: Text('profile.title'.tr()))`
- Linha 56: `Text('Papel: ${me['role'] ?? '-'}', …)` → `Text('profile.role'.tr(namedArgs: {'role': '${me['role'] ?? '-'}'}), …)`
- Linha 72: `Text('$pts pontos', …)` → `Text('profile.points'.tr(namedArgs: {'n': '$pts'}), …)`
- Linha 77: `Text(next != null ? 'faltam $next para o próximo Tier' : 'Tier máximo!', …)` → `Text(next != null ? 'profile.to_next_tier'.tr(namedArgs: {'n': '$next'}) : 'profile.max_tier'.tr(), …)`
- Linha 83: `Text('Minhas raids', …)` → `Text('profile.my_raids'.tr(), …)`
- Linha 90: `const Text('Você ainda não participou de raids.')` → `Text('profile.no_raids'.tr())`
- Linhas 94-96 (badge): `'Criador'`/`'Confirmado'`/`'Waitlist'` → `'common.creator'.tr()`/`'common.confirmed'.tr()`/`'common.waitlist'.tr()`

Aplicar o mesmo padrão nas outras 5 telas (characters, character_form, character_profile, me_progression, users_admin).

- [ ] **Step 4: Paridade + analyze**

Run (PowerShell): `cd app; flutter test test/l10n_key_parity_test.dart; flutter analyze`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add app/lib/features/characters app/lib/features/profile app/lib/features/admin app/assets/translations
git commit -m "i18n(app): migra characters, profile, progression e admin"
```

---

## Task 9: Varredura final por strings esquecidas + ajuste dos widget tests existentes

**Files:**
- Modify: quaisquer telas com literais remanescentes
- Modify: `app/test/**` (widget tests existentes que quebraram com o wrapper de localização)

- [ ] **Step 1: Varredura por literais de UI remanescentes**

Run (PowerShell/Bash): buscar `Text('...literal...')`, `label:`, `hintText:`, `labelText:`, `title:` com literais que contenham letras acentuadas ou palavras em PT/EN não migradas:
```bash
grep -rnE "(Text\(|label:|hintText:|labelText:|title:) *'[A-Za-zÀ-ÿ]" app/lib --include="*.dart" | grep -v "\.tr()"
```
Expected: nenhuma linha de UI voltada ao usuário (só keys internas legítimas). Migrar o que sobrar.

- [ ] **Step 2: Rodar toda a suíte de testes e corrigir os que quebraram**

Run (PowerShell): `cd app; flutter test`
Expected: alguns widget tests existentes podem falhar por falta de contexto de localização. Corrigir cada um usando o helper `pumpLocalized` (Task 2) e chamando `EasyLocalization.ensureInitialized()` no `setUpAll`.

- [ ] **Step 3: analyze limpo**

Run (PowerShell): `cd app; flutter analyze`
Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add app
git commit -m "i18n(app): varredura final de strings + ajusta widget tests"
```

---

## Task 10: Backend — normalizar mensagens para inglês

**Files:**
- Modify: `backend/src/common/errors/AppError.ts`, `common/middleware/errorHandler.ts`, `common/middleware/validate.ts`, `common/security/jwt.ts`, `discord/discordSync.ts`, e todos `modules/**/*.service.ts` + `modules/**/*.schemas.ts` com literais PT.

**Interfaces:**
- Manter **inalterados** os códigos/chaves de erro internos (ex.: `'BAD_REQUEST'`, `'NOT_FOUND'`); mudar apenas o **texto legível**.

- [ ] **Step 1: Mapear todas as mensagens PT**

Run (Bash): 
```bash
grep -rnoE "'[^']*(não|já|inválid|obrigatóri|erro|sucesso|criad|encontrad|permiss|usuári|senha|falh|conclu|pertence|permitid|bate|está|Recurso|Requisição|Payload|claims|Token|Personagem|Raid|Você|Disciplina|Combat|Role|Origin)[^']*'" backend/src --include="*.ts"
```
Expected: lista das linhas a traduzir.

- [ ] **Step 2: Substituir por inglês (mapa de referência)**

Aplicar estas traduções (e quaisquer outras encontradas no Step 1, seguindo o mesmo tom):
```
'Requisição inválida'                     → 'Invalid request'
'Sem permissão'                           → 'Forbidden'
'Não encontrado'                          → 'Not found'
'Payload inválido'                        → 'Invalid payload'
'Recurso não encontrado'                  → 'Resource not found'
'Erro não tratado'                        → 'Unhandled error'
'claims inválidas'                        → 'Invalid claims'
'Token inválido ou expirado'              → 'Invalid or expired token'
'discord sync falhou'                     → 'Discord sync failed'
'Refresh inválido'                        → 'Invalid refresh token'
'Combat Style não pertence à facção'      → 'Combat Style does not belong to the faction'
'Role não permitida para a classe'        → 'Role not allowed for this class'
'Origin Story não bate com a classe'      → 'Origin Story does not match the class'
'Disciplina não pertence à classe'        → 'Discipline does not belong to the class'
'Combat Style inválido para a facção'     → 'Invalid Combat Style for the faction'
'Personagem não encontrado'               → 'Character not found'
'Personagem de outro usuário'             → 'Character belongs to another user'
'Usuário não encontrado'                  → 'User not found'
'Raid não encontrada'                     → 'Raid not found'
'A raid não está aberta para inscrições'  → 'This raid is not open for sign-ups'
'Você já está nesta raid'                 → 'You are already in this raid'
'Você não está nesta raid'                → 'You are not in this raid'
```

- [ ] **Step 3: Build/lint do backend**

Run (PowerShell): `cd backend; npm run build`
Expected: compila sem erro. Se houver script de lint (`npm run lint`), rodar também.

- [ ] **Step 4: Varredura de confirmação**

Run (Bash):
```bash
grep -rnoiE "(não|já |inválid|obrigatóri|usuári|senha|falh|conclu|criad|encontrad|pertence|permitid)" backend/src --include="*.ts" | grep -iE "'.*(não|já|inválid|usuári)"
```
Expected: nenhuma mensagem PT voltada ao usuário remanescente (comentários em PT no código podem ficar).

- [ ] **Step 5: Commit**

```bash
git add backend/src
git commit -m "chore(backend): normaliza mensagens ao usuario para ingles"
```

---

## Task 11: Verificação final e smoke manual

- [ ] **Step 1: Suíte completa + analyze do app**

Run (PowerShell): `cd app; flutter test; flutter analyze`
Expected: todos os testes PASS (incluindo paridade de chaves); analyze limpo.

- [ ] **Step 2: Build do backend**

Run (PowerShell): `cd backend; npm run build`
Expected: sucesso.

- [ ] **Step 3: Smoke manual do app**

Run (PowerShell): `cd app; flutter run -d chrome`
Verificar:
- App abre em inglês (ou no idioma do dispositivo, se suportado).
- Trocar idioma no seletor do **Login** muda a UI imediatamente.
- Após login, trocar idioma no **Perfil** muda a UI.
- Recarregar/reabrir o app mantém o idioma escolhido (persistência).
- Passar por todas as telas confirmando que não há texto não traduzido.

- [ ] **Step 4: Commit final (se houver ajustes) e fechar**

```bash
git add -A
git commit -m "i18n: verificacao final e ajustes de smoke"
```

---

## Notas de execução

- **Ordem de dependência:** Task 1 → 2 → (3..8 podem intercalar, mas mantêm paridade a cada commit) → 9 → 10 → 11. Tasks 3–8 dependem do wiring da Task 1 e do teste da Task 2.
- **Paridade sempre verde:** ao adicionar chaves em `en.json`, adicionar simultaneamente nos 4 outros. O teste da Task 2 é a rede de segurança — rodá-lo a cada tarefa.
- **Qualidade das traduções de/fr/es:** funcional inicial; revisão por nativo é follow-up fora deste plano.
- **Limitação consciente:** mensagens de erro cruas da API ficam só em inglês (backend sem i18n).
