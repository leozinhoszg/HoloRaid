# Perfil pessoal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Uma tela de perfil do próprio usuário (identidade + resumo dos personagens + minhas raids), com a única adição de backend sendo `GET /me/raids`.

**Architecture:** `raidRepo.listForUser(userId)` faz uma query única (`LEFT JOIN raid_players` na minha linha, `WHERE created_by = me OR participei`) devolvendo cada raid uma vez com `created`/`myStatus`. Um controller fino expõe `GET /me/raids`. A tela Flutter compõe `/me` + `/characters` (soma pontos no cliente) + `/me/raids`.

**Tech Stack:** Node/TypeScript, Kysely + mysql2, Express, vitest + supertest; Flutter (Riverpod, go_router, Dio).

## Global Constraints

- **Spec:** `docs/superpowers/specs/2026-07-17-profile-design.md`.
- **Só o próprio usuário:** `/me/raids` usa **sempre** `req.user.sub` — o cliente nunca passa id de usuário.
- **Total PvE agregado é calculado no CLIENTE** (soma de `total_points`, max `tier` do `/characters`). Sem endpoint agregado novo.
- **Progressão PvE agregada e "World Bosses" ficam FORA** (já existe por personagem / não modelado).
- **`created` vem do MySQL como 0/1** (expressão booleana) → `norm` força `!!Number(row.created)`.
- **A lógica de união (criei ∪ entrei, myStatus, ordenação) é uma query SQL** — provada por teste contra o **MySQL real**, não por fake (o fake do `raidRepo` não conhece o roster). O teste de rota usa um stub e cobre 401 + passthrough.
- **`profileRaidRepo` opcional no `createApp`** → #1–#7 intactos.
- **Regressão:** os **208 testes** de #1–#7 seguem verdes.
- **Commits:** autor `Leonardo de Souza Guimarães <leozinhoszg@gmail.com>`. **NUNCA** `Co-Authored-By: Claude`.
- Backend em `backend/`. Testes: `npx vitest run <arquivo>`. Typecheck: `npm run typecheck > /dev/null 2>&1; echo "EXIT=$?"` (**não use pipe**). Prefixe comandos com `cd /d/HoloRaid/backend &&`.

---

### Task 1: `raidRepo.listForUser` (query de união)

**Files:**
- Modify: `backend/src/db/repositories/raidRepo.ts` (tipo `MyRaid`, interface, impl)
- Modify: `backend/tests/fakes/fakeRepos.ts` (`makeFakeRaidRepo` — impl mínima p/ satisfazer o tipo)
- Test: `backend/tests/profile.listForUser.test.ts` (novo — **MySQL real**)

**Interfaces:**
- Consumes: tabelas `raids`/`raid_players`.
- Produces:
  - `export type MyRaid = { id: number; codigo: string; operation: string; difficulty: Difficulty; size: number; faction: Faction; start_at: Date; status: RaidStatus; created: boolean; myStatus: 'confirmed' | 'waitlist' | null }`
  - `RaidRepo.listForUser(userId: number): Promise<MyRaid[]>`

> **Fake incompleto de propósito.** `makeFakeRaidRepo` não tem dados de `raid_players`, então
> seu `listForUser` só sabe o lado "criador" (`created_by === userId` → `created:true,
> myStatus:null`). Isso satisfaz o tipo (outros testes constroem `raidService` com o fake) e é
> honesto; a união completa é provada pelo teste real desta task.

- [ ] **Step 1: Write the failing test (MySQL real)**

Crie `backend/tests/profile.listForUser.test.ts`:

```ts
import 'dotenv/config';
import { db } from '../src/db/db';
import { createRaidRepo } from '../src/db/repositories/raidRepo';
import { createRaidPlayerRepo } from '../src/db/repositories/raidPlayerRepo';
import { createPersonagemRepo } from '../src/db/repositories/personagemRepo';
import { createUserRepo } from '../src/db/repositories/userRepo';

const raidRepo = createRaidRepo(db);
const raidPlayerRepo = createRaidPlayerRepo(db);
const personagemRepo = createPersonagemRepo(db);
const userRepo = createUserRepo(db);

const MARK = 'PROF8';
const created = { users: [] as number[], raids: [] as number[] };

async function mkUser(tag: string) {
  const u = await userRepo.upsertByDiscordId({ discord_id: `${MARK}-${tag}`, username: `${MARK}_${tag}`, nickname: null, avatar: null, email: null, role: 'user' });
  created.users.push(u.id); return u;
}
async function mkRaid(startAt: Date, createdBy: number) {
  const r = await raidRepo.create({ codigo: `${MARK}${created.raids.length}`, operation: 'Dread Palace', difficulty: 'HM', size: 8, faction: 'Republic', minimum_tier: 0, check_composition: false, slots_tank: 2, slots_heal: 2, slots_dps: 4, notes: null, start_at: startAt, created_by: createdBy } as any);
  created.raids.push(r.id); return r;
}

afterAll(async () => {
  for (const id of created.raids) await raidRepo.delete(id);
  if (created.users.length) await db.deleteFrom('usuarios').where('id', 'in', created.users).execute();
  await db.destroy();
});

describe('raidRepo.listForUser', () => {
  it('une raids criadas e entradas, com created/myStatus e ordem desc', async () => {
    const me = await mkUser('me');
    const other = await mkUser('other');
    const meChar = await personagemRepo.create({ usuario_id: me.id, nome: 'Me', faccao: 'Republic', classe: 'Guardian', especializacao: null, role: 'Tank', origin_story: null, item_level: 340 } as any);

    const base = Date.now();
    const rCriei = await mkRaid(new Date(base + 3 * 3600_000), me.id);                 // criei, não entrei
    const rEntrei = await mkRaid(new Date(base + 2 * 3600_000), other.id);             // entrei (não criei)
    const rAmbos = await mkRaid(new Date(base + 1 * 3600_000), me.id);                 // criei e entrei
    const rAlheia = await mkRaid(new Date(base + 4 * 3600_000), other.id);             // nada meu

    await raidPlayerRepo.create({ raid_id: rEntrei.id, usuario_id: me.id, personagem_id: meChar.id, role: 'Tank', status: 'confirmed', joined_at: new Date() });
    await raidPlayerRepo.create({ raid_id: rAmbos.id, usuario_id: me.id, personagem_id: meChar.id, role: 'Tank', status: 'waitlist', joined_at: new Date() });

    const mine = await raidRepo.listForUser(me.id);
    const byId = new Map(mine.map((r) => [r.id, r]));

    expect(byId.has(rAlheia.id)).toBe(false);
    expect(byId.get(rCriei.id)).toMatchObject({ created: true, myStatus: null });
    expect(byId.get(rEntrei.id)).toMatchObject({ created: false, myStatus: 'confirmed' });
    expect(byId.get(rAmbos.id)).toMatchObject({ created: true, myStatus: 'waitlist' });

    // cada raid uma vez
    const nossos = mine.filter((r) => [rCriei.id, rEntrei.id, rAmbos.id].includes(r.id));
    expect(nossos).toHaveLength(3);

    // ordem desc por start_at: rCriei(+3h) antes de rEntrei(+2h) antes de rAmbos(+1h)
    const idxCriei = nossos.findIndex((r) => r.id === rCriei.id);
    const idxAmbos = nossos.findIndex((r) => r.id === rAmbos.id);
    expect(idxCriei).toBeLessThan(idxAmbos);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && npx vitest run tests/profile.listForUser.test.ts`
Expected: FAIL — `raidRepo.listForUser is not a function`.

- [ ] **Step 3: Tipo + interface + impl no repo**

Em `backend/src/db/repositories/raidRepo.ts`, garanta o import do `sql` no topo (o arquivo hoje só importa `type { Kysely }`):

```ts
import { sql, type Kysely } from 'kysely';
```

adicione o tipo (depois de `NewRaid`):

```ts
export type MyRaid = {
  id: number; codigo: string; operation: string; difficulty: Difficulty; size: number;
  faction: Faction; start_at: Date; status: RaidStatus;
  created: boolean; myStatus: 'confirmed' | 'waitlist' | null;
};
```

à interface `RaidRepo` (depois de `list`):

```ts
  listForUser(userId: number): Promise<MyRaid[]>;
```

e ao `createRaidRepo` (antes de `update`):

```ts
    // Raids do usuário: criadas (created_by) OU entradas (raid_players). Uma linha por raid
    // graças ao UNIQUE (raid_id, usuario_id); myStatus null quando só criou.
    async listForUser(userId) {
      const rows = await db.selectFrom('raids as r')
        .leftJoin('raid_players as rp', (join) =>
          join.onRef('rp.raid_id', '=', 'r.id').on('rp.usuario_id', '=', userId))
        .select([
          'r.id as id', 'r.codigo as codigo', 'r.operation as operation', 'r.difficulty as difficulty',
          'r.size as size', 'r.faction as faction', 'r.start_at as start_at', 'r.status as status',
          sql<number>`(r.created_by = ${userId})`.as('created'),
          'rp.status as my_status',
        ])
        .where((eb) => eb.or([eb('r.created_by', '=', userId), eb('rp.usuario_id', '=', userId)]))
        .orderBy('r.start_at', 'desc')
        .execute();
      return rows.map((row: any) => ({
        id: row.id, codigo: row.codigo, operation: row.operation, difficulty: row.difficulty,
        size: row.size, faction: row.faction, start_at: new Date(row.start_at), status: row.status,
        created: !!Number(row.created), myStatus: row.my_status ?? null,
      }));
    },
```

- [ ] **Step 4: Impl mínima no fake**

Em `backend/tests/fakes/fakeRepos.ts`, no `makeFakeRaidRepo`, adicione (depois de `list`):

```ts
    // fake não conhece o roster: só o lado "criador". A união real é testada contra o MySQL.
    async listForUser(userId) {
      return rows.filter((r) => r.created_by === userId).map((r) => ({
        id: r.id, codigo: r.codigo, operation: r.operation, difficulty: r.difficulty,
        size: r.size, faction: r.faction, start_at: r.start_at, status: r.status,
        created: true, myStatus: null as 'confirmed' | 'waitlist' | null,
      }));
    },
```

- [ ] **Step 5: Run test to verify it passes**

Run: `cd backend && npx vitest run tests/profile.listForUser.test.ts && npm run typecheck > /dev/null 2>&1; echo "EXIT=$?"`
Expected: PASS e `EXIT=0`.

- [ ] **Step 6: Commit**

```bash
git add backend/src/db/repositories/raidRepo.ts backend/tests/fakes/fakeRepos.ts backend/tests/profile.listForUser.test.ts
git commit -m "feat(profile): raidRepo.listForUser (uniao criadas + entradas)"
```

---

### Task 2: Endpoint `GET /me/raids`

**Files:**
- Create: `backend/src/modules/profile/profile.controller.ts`
- Create: `backend/src/modules/profile/profile.router.ts`
- Modify: `backend/src/app.ts`
- Modify: `backend/src/server.ts`
- Test: `backend/tests/profile.routes.test.ts`

**Interfaces:**
- Consumes: `RaidRepo.listForUser` (Task 1), `requireAuth` (#1).
- Produces:
  - `createProfileRouter(raidRepo: RaidRepo): Router` → `GET /me/raids`.
  - `createApp({ ..., profileRaidRepo?: RaidRepo })`.

- [ ] **Step 1: Write the failing test**

Crie `backend/tests/profile.routes.test.ts`:

```ts
import request from 'supertest';
import { createApp } from '../src/app';
import { signAccessToken } from '../src/common/security/jwt';

beforeAll(() => { process.env.JWT_SECRET = 'z'.repeat(40); process.env.DISCORD_CLIENT_ID = 'C'; process.env.DISCORD_CLIENT_SECRET = 'S'; process.env.DISCORD_REDIRECT_URI = 'http://localhost/cb'; });

// stub: só listForUser é chamado pelo controller; registra o userId recebido
function stubRepo(spy?: (uid: number) => void) {
  return {
    async listForUser(uid: number) {
      spy?.(uid);
      return [{ id: 1, codigo: 'AB12', operation: 'Dread Palace', difficulty: 'HM', size: 8, faction: 'Republic', start_at: new Date('2026-08-01T20:30:00Z'), status: 'OPEN', created: true, myStatus: null }];
    },
  } as any;
}
const tok = (sub = 7) => signAccessToken({ sub, role: 'user' });

describe('GET /me/raids', () => {
  it('sem JWT → 401', async () => {
    const app = createApp({ authService: {} as any, profileRaidRepo: stubRepo() });
    expect((await request(app).get('/me/raids')).status).toBe(401);
  });

  it('com JWT → 200, usa o sub do token e devolve a lista', async () => {
    let uid = 0;
    const app = createApp({ authService: {} as any, profileRaidRepo: stubRepo((u) => { uid = u; }) });
    const res = await request(app).get('/me/raids').set('Authorization', `Bearer ${tok(7)}`);
    expect(res.status).toBe(200);
    expect(uid).toBe(7); // usou o sub do JWT, não um id do cliente
    expect(res.body).toHaveLength(1);
    expect(res.body[0]).toMatchObject({ codigo: 'AB12', created: true, myStatus: null });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && npx vitest run tests/profile.routes.test.ts`
Expected: FAIL — `404` no `GET /me/raids`.

- [ ] **Step 3: Controller**

Crie `backend/src/modules/profile/profile.controller.ts`:

```ts
import type { Request, Response } from 'express';
import type { RaidRepo } from '../../db/repositories/raidRepo';

export function createProfileController(raidRepo: RaidRepo) {
  return {
    async myRaids(req: Request, res: Response) {
      res.json(await raidRepo.listForUser(req.user!.sub));
    },
  };
}
```

- [ ] **Step 4: Router**

Crie `backend/src/modules/profile/profile.router.ts`:

```ts
import { Router } from 'express';
import { requireAuth } from '../../common/security/guards';
import { createProfileController } from './profile.controller';
import type { RaidRepo } from '../../db/repositories/raidRepo';

const wrap = (fn: (req: any, res: any) => Promise<unknown> | unknown) =>
  (req: any, res: any, next: any) => Promise.resolve(fn(req, res)).catch(next);

export function createProfileRouter(raidRepo: RaidRepo): Router {
  const c = createProfileController(raidRepo);
  const r = Router();
  r.get('/me/raids', requireAuth, wrap(c.myRaids));
  return r;
}
```

- [ ] **Step 5: Montar no app**

Em `backend/src/app.ts`:

```ts
import { createProfileRouter } from './modules/profile/profile.router';
import type { RaidRepo } from './db/repositories/raidRepo';
```
adicione `profileRaidRepo?: RaidRepo;` ao `deps` e monte (junto dos outros `if`):

```ts
  if (deps.profileRaidRepo) app.use('/', createProfileRouter(deps.profileRaidRepo));
```

> **Ordem de rotas:** monte o profile router **antes** do `createUsersRouter` **não** é
> necessário — `/me/raids` e `/me` são paths distintos e o Express casa por path exato. Ordem
> livre.

- [ ] **Step 6: Run test to verify it passes**

Run: `cd backend && npx vitest run tests/profile.routes.test.ts && npm run typecheck > /dev/null 2>&1; echo "EXIT=$?"`
Expected: 2 testes PASS e `EXIT=0`.

- [ ] **Step 7: Wiring real no server**

Em `backend/src/server.ts`, passe o `raidRepo` (que já existe) ao `createApp`:

```ts
const app = createApp({ …, dashboardService, profileRaidRepo: raidRepo });
```
(adicione `profileRaidRepo: raidRepo` ao objeto já existente do `createApp`.)

- [ ] **Step 8: Commit**

```bash
git add backend/src/modules/profile backend/src/app.ts backend/src/server.ts backend/tests/profile.routes.test.ts
git commit -m "feat(profile): endpoint GET /me/raids"
```

---

### Task 3: Tela Flutter `ProfileScreen`

**Files:**
- Create: `app/lib/features/profile/profile_screen.dart`
- Modify: `app/lib/core/router/app_router.dart`
- Modify: `app/lib/features/home/home_screen.dart`

**Interfaces:**
- Consumes: `authServiceProvider.loadMe()`, `charactersProvider` (`features/characters/characters_providers.dart`), `apiClientProvider` (para `/me/raids`).
- Produces: rota `/profile`.

> Sem widget test. Verificação: `flutter analyze` + smoke manual.

- [ ] **Step 1: Criar a tela**

Crie `app/lib/features/profile/profile_screen.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import '../../core/auth/auth_providers.dart';
import '../characters/characters_providers.dart';

class ProfileScreen extends ConsumerStatefulWidget {
  const ProfileScreen({super.key});
  @override
  ConsumerState<ProfileScreen> createState() => _ProfileScreenState();
}

class _ProfileScreenState extends ConsumerState<ProfileScreen> {
  late Future<Map<String, dynamic>> _me;
  late Future<List<dynamic>> _raids;

  @override
  void initState() {
    super.initState();
    _me = ref.read(authServiceProvider).loadMe();
    _raids = _loadRaids();
  }

  Future<List<dynamic>> _loadRaids() async {
    final res = await ref.read(apiClientProvider).dio.get('/me/raids');
    return (res.data as List).cast<dynamic>();
  }

  @override
  Widget build(BuildContext context) {
    final chars = ref.watch(charactersProvider);
    return Scaffold(
      appBar: AppBar(title: const Text('Perfil')),
      body: FutureBuilder<Map<String, dynamic>>(
        future: _me,
        builder: (context, meSnap) {
          if (!meSnap.hasData) return const Center(child: CircularProgressIndicator());
          final me = meSnap.data!;
          return ListView(
            padding: const EdgeInsets.all(16),
            children: [
              Row(children: [
                CircleAvatar(radius: 28, child: Text((me['username'] as String? ?? '?').substring(0, 1).toUpperCase())),
                const SizedBox(width: 12),
                Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                  Text(me['username'] as String? ?? '—', style: Theme.of(context).textTheme.titleLarge),
                  Text('Papel: ${me['role'] ?? '-'}', style: Theme.of(context).textTheme.bodySmall),
                ]),
              ]),
              const SizedBox(height: 20),
              chars.when(
                loading: () => const LinearProgressIndicator(),
                error: (e, _) => Text('Erro nos personagens: $e'),
                data: (list) {
                  final totalPontos = list.fold<int>(0, (s, c) => s + c.totalPoints);
                  final maiorTier = list.fold<int>(0, (m, c) => c.tier > m ? c.tier : m);
                  return Card(child: Padding(
                    padding: const EdgeInsets.all(16),
                    child: Row(mainAxisAlignment: MainAxisAlignment.spaceAround, children: [
                      _mini('Personagens', '${list.length}'),
                      _mini('Pontos PvE', '$totalPontos'),
                      _mini('Maior Tier', maiorTier == 0 ? '—' : 'T$maiorTier'),
                    ]),
                  ));
                },
              ),
              const SizedBox(height: 20),
              Text('Minhas raids', style: Theme.of(context).textTheme.titleMedium),
              const SizedBox(height: 8),
              FutureBuilder<List<dynamic>>(
                future: _raids,
                builder: (context, rSnap) {
                  if (!rSnap.hasData) return const Center(child: Padding(padding: EdgeInsets.all(16), child: CircularProgressIndicator()));
                  final raids = rSnap.data!;
                  if (raids.isEmpty) return const Text('Você ainda não participou de raids.');
                  return Column(children: raids.map((r) {
                    final m = (r as Map).cast<String, dynamic>();
                    final quando = DateTime.parse(m['start_at'] as String).toLocal();
                    final badge = (m['created'] as bool? ?? false)
                        ? 'Criador'
                        : (m['myStatus'] == 'confirmed' ? 'Confirmado' : m['myStatus'] == 'waitlist' ? 'Waitlist' : '—');
                    return ListTile(
                      dense: true,
                      title: Text('${m['operation']} · ${m['difficulty']}'),
                      subtitle: Text('${quando.toString().substring(0, 16)} · ${m['status']}'),
                      trailing: Chip(label: Text(badge)),
                      onTap: () => context.push('/raids/${m['id']}'),
                    );
                  }).toList());
                },
              ),
            ],
          );
        },
      ),
    );
  }

  Widget _mini(String label, String value) => Column(children: [
    Text(value, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),
    Text(label, style: const TextStyle(fontSize: 12)),
  ]);
}
```

- [ ] **Step 2: Registrar a rota**

Em `app/lib/core/router/app_router.dart`, adicione o import e a rota:

```dart
import '../../features/profile/profile_screen.dart';
```
```dart
      GoRoute(path: '/profile', builder: (_, _) => const ProfileScreen()),
```

- [ ] **Step 3: Botão na home**

Em `app/lib/features/home/home_screen.dart`, adicione (depois do botão "Dashboard"):

```dart
                const SizedBox(height: 12),
                FilledButton.icon(
                  onPressed: () => context.push('/profile'),
                  icon: const Icon(Icons.person),
                  label: const Text('Perfil'),
                ),
```

- [ ] **Step 4: Verificar**

Run: `cd app && flutter analyze`
Expected: `No issues found!`

- [ ] **Step 5: Commit**

```bash
git add app/lib/features/profile/profile_screen.dart app/lib/core/router/app_router.dart app/lib/features/home/home_screen.dart
git commit -m "feat(app): tela de Perfil (identidade + resumo + minhas raids)"
```

---

### Task 4: Verificação final

**Files:** nenhum (verificação).

- [ ] **Step 1: Suíte completa + typecheck + build**

Run: `cd backend && npm test`
Expected: **todos passam**. Antes eram 208; o plano acrescenta **3** (1 listForUser + 2 rota) → espere **211 passed, 0 failed**.

Run: `cd backend && npm run typecheck > /dev/null 2>&1; echo "typecheck EXIT=$?"; npm run build > /dev/null 2>&1; echo "build EXIT=$?"`
Expected: ambos `EXIT=0`.

- [ ] **Step 2: Smoke real do endpoint via HTTP**

Sobe o server e bate em `/me/raids` com um JWT real, provando a rota fim-a-fim. Usa o poll de
saúde (o boot leva ~5s):

```bash
cd backend && cat > smoke.tmp.ts <<'EOF'
import 'dotenv/config';
import { db } from './src/db/db';
import { createUserRepo } from './src/db/repositories/userRepo';
import { createPersonagemRepo } from './src/db/repositories/personagemRepo';
import { createRaidRepo } from './src/db/repositories/raidRepo';
import { createRaidPlayerRepo } from './src/db/repositories/raidPlayerRepo';

(async () => {
  const userRepo = createUserRepo(db);
  const personagemRepo = createPersonagemRepo(db);
  const raidRepo = createRaidRepo(db);
  const raidPlayerRepo = createRaidPlayerRepo(db);

  const me = await userRepo.upsertByDiscordId({ discord_id: 'PROF8s', username: 'PROF8s', nickname: null, avatar: null, email: null, role: 'user' });
  const other = await userRepo.upsertByDiscordId({ discord_id: 'PROF8o', username: 'PROF8o', nickname: null, avatar: null, email: null, role: 'user' });
  const ch = await personagemRepo.create({ usuario_id: me.id, nome: 'M', faccao: 'Republic', classe: 'Guardian', especializacao: null, role: 'Tank', origin_story: null, item_level: 340 } as any);
  const rCriei = await raidRepo.create({ codigo: 'PROF8A', operation: 'Dread Palace', difficulty: 'HM', size: 8, faction: 'Republic', minimum_tier: 0, check_composition: false, slots_tank: 2, slots_heal: 2, slots_dps: 4, notes: null, start_at: new Date(Date.now() + 3600_000), created_by: me.id } as any);
  const rEntrei = await raidRepo.create({ codigo: 'PROF8B', operation: 'Dread Palace', difficulty: 'HM', size: 8, faction: 'Republic', minimum_tier: 0, check_composition: false, slots_tank: 2, slots_heal: 2, slots_dps: 4, notes: null, start_at: new Date(Date.now() + 7200_000), created_by: other.id } as any);
  await raidPlayerRepo.create({ raid_id: rEntrei.id, usuario_id: me.id, personagem_id: ch.id, role: 'Tank', status: 'confirmed', joined_at: new Date() });

  const mine = await raidRepo.listForUser(me.id);
  const ids = mine.map((r) => r.codigo);
  const a = mine.find((r) => r.codigo === 'PROF8A');
  const b = mine.find((r) => r.codigo === 'PROF8B');
  console.log('--> minhas raids:', JSON.stringify(ids.filter((c) => c.startsWith('PROF8'))));
  console.log('--> PROF8A (criei):', a?.created, a?.myStatus, '(esperado true / null)');
  console.log('--> PROF8B (entrei):', b?.created, b?.myStatus, '(esperado false / confirmed)');

  const ok = a?.created === true && a?.myStatus === null && b?.created === false && b?.myStatus === 'confirmed';

  await raidRepo.delete(rCriei.id); await raidRepo.delete(rEntrei.id);
  await db.deleteFrom('usuarios').where('id', 'in', [me.id, other.id]).execute();
  console.log(ok ? '\n=== SMOKE OK ===' : '\n=== SMOKE FALHOU ===');
  await db.destroy();
  process.exit(ok ? 0 : 1);
})().catch((e) => { console.log('FALHOU:', e.code || '', e.sqlMessage || e.message); process.exit(1); });
EOF
npx tsx smoke.tmp.ts; rm -f smoke.tmp.ts
```
Expected: `PROF8A` com `true/null`, `PROF8B` com `false/confirmed`, `=== SMOKE OK ===`.

- [ ] **Step 3: Commit (se algum ajuste foi necessário)**

```bash
git add -A
git commit -m "test(profile): verificacao (regressao + smoke)"
```

---

## Notas de execução

- **Branch:** `feat/profile`, merge `--no-ff` na `master` ao final (skill `finishing-a-development-branch`).
- **Ordem:** 1 → 2 → 3 → 4. A Task 3 (Flutter) depende do endpoint da Task 2.
- **Teste `profile.listForUser` toca o MySQL real** (a união é SQL). Precisa do banco no ar.
- **Cuidado com o `cd`** e **não use pipe no typecheck**.
